import { motion } from "framer-motion";

export default function ComingSoon() {
  return (
    <motion.div>
      <motion.h1 className="text-3xl font-bold text-textBlue">
        Norsang is Currently Fixing This Page. Please check back later!
      </motion.h1>
    </motion.div>
  );
}
